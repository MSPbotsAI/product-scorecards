// app.ts — app init `setup` (runs once at startup, after runtime globals, before first render)
// plus optional route guards (`beforeEach` / `afterEach`). See the @mspbots/react README.
//
// Nothing to register yet: the sidebar is driven by `pages/**/page.tsx` meta. Add `registerMenu`
// calls here when the scorecard needs entries that aren't file pages (e.g. an L10 external link).

export function setup() {}
